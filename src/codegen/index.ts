// Génération de code (Python SDK, curl, JavaScript fetch) — ticket T4.
// Invariant : fonctions pures (LLMRequest → string), testées par snapshots,
// la clé API y est toujours remplacée par un placeholder.

export {}
