// Valeurs par défaut partagées par les niveaux.

/**
 * Prompt fil rouge du workshop : le verbatim client télécom, réutilisé de
 * L1 à L4 pour comparer les niveaux sur le même cas.
 */
export const FIL_ROUGE_PROMPT =
  'Analyse ce verbatim client : "Impossible de joindre le service client ' +
  'depuis 3 jours, le chat coupe à chaque fois, je songe à résilier."'

/** Plafond de tokens de sortie par défaut (devient réglable au niveau 2). */
export const DEFAULT_MAX_TOKENS = 1024
